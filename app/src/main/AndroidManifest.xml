<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="activity.project.veviuasqr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.veviwulandari">
        <activity android:name="activity.project.veviuasqr.ScanActivity" />
        <activity android:name="activity.project.veviuasqr.LoginActivity" />
        <activity android:name="activity.project.veviuasqr.getStarted" />
        <activity android:name="activity.project.veviuasqr.DaftarActivity" />
        <activity android:name="activity.project.veviuasqr.DataActivity" />
        <activity android:name="activity.project.veviuasqr.MainActivity" />
        <activity
            android:name="activity.project.veviuasqr.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>